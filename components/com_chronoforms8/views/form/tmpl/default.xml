<?xml version="1.0" encoding="utf-8"?>
<metadata>
	<layout title="Display a Form" option="Display a Form">
		<help key = "Display a Form" />
		<message>
			Enter a form alias to display it.
		</message>
	</layout>

	<!-- Add fields to the parameters object for the layout. -->
	<fields name="params">
		<!-- Basic options. -->
		<fieldset name="basic" label="COM_MENUS_BASIC_FIELDSET_LABEL">
			<field name="chronoform" type="text" description="Enter your form's alias, make sure you use the alias, not the form's title" label="Form's ALIAS"></field>
		</fieldset>
		<fieldset name="basic" label="COM_MENUS_BASIC_FIELDSET_LABEL">
			<field name="form_params" type="text" description="Extra params to pass to the form as data, example: p1=v1" label="Extra parameters"></field>
		</fieldset>
	</fields>
</metadata>